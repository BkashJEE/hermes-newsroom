/**
 * Product identity. The name is a working title — change it here (and in
 * package.json) to rename the app everywhere.
 */
export const APP = {
  name: "Omarchy Command Center",
  shortName: "Command Center",
  description: "A Hermes-driven command center for an Omarchy desktop. Each tab is a page with one job.",
} as const;
